{
  "name": "touchdown",
  "version": "2.0.0",
  "type": "module",
  "description": "Connect NFL players by their mutual teammates",
  "scripts": {
    "dev": "bun --hot ./src/index.html",
    "build": "bun build ./src/index.html --outdir=dist --sourcemap=external --target=browser --minify",
    "lint": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "dependencies": {
    "preact": "^10.19.3",
    "radix-ui": "^1.4.3",
    "react": "npm:@preact/compat",
    "react-dom": "npm:@preact/compat"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.3.3"
  }
}
